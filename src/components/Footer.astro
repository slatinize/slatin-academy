---
import { ui } from '../data/ui';
import type { Locale } from '../utils/i18n';

interface Props { lang?: Locale }
const lang: Locale = Astro.props.lang ?? 'en';
const t = ui[lang].footer;
---

<!-- Gradient Footer -->
<footer>
  <div class="bg-gradient-to-r from-accent-yellow via-accent-orange to-accent-red py-16 px-6 md:px-12 lg:px-20">
    <div class="container-content flex flex-col md:flex-row justify-between gap-10">
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-extrabold text-dark leading-tight max-w-md">
        {t.headline}
      </h2>
      <div class="flex gap-16">
        <div>
          <h3 class="font-bold text-dark mb-3">{t.quickJumpTitle}</h3>
          <ul class="space-y-2">
            {t.quickLinks.map((link) => (
              <li>
                <a href={link.href} class="text-dark underline hover:no-underline font-medium">
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
        <div>
          <p class="font-bold text-dark mb-3 text-sm tracking-wide">SST PRO DYNAMICS SLL</p>
          <img
            src="https://www.slatin.pro/wp-content/uploads/2024/03/ADM-Plural-Album-Large-en_US-300x90.png"
            alt="Apple Digital Masters"
            class="h-8 w-auto"
          />
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom bar -->
  <div class="bg-dark-secondary py-4 px-6 md:px-12 lg:px-20">
    <div class="container-content flex flex-wrap justify-between gap-4 text-sm text-white/50">
      {t.legalLinks.map((link) => (
        <a href={link.href} class="hover:text-white/80 underline transition-colors">
          {link.label}
        </a>
      ))}
    </div>
  </div>
</footer>
