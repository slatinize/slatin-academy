---
interface Props {
  text: string;
  subtitle?: string;
  id?: string;
}

const { text, subtitle, id } = Astro.props;
---

<section class="section-dark pt-[3.8rem] pb-[3rem] md:pt-[4.5rem] md:pb-[4rem] relative" id={id}>
  <div class="section-grid" aria-hidden="true"></div>
  <!-- Scrolling text marquee -->
  <div class="marquee-clip">
    <div class="flex whitespace-nowrap animate-marquee-banner">
      {[...Array(6)].map(() => (
        <span class="marquee-text text-6xl md:text-8xl lg:text-9xl mx-6">
          {text}
        </span>
      ))}
    </div>
  </div>

  {subtitle && (
    <div class="text-center px-6 pt-8 reveal">
      <h2 class="text-2xl md:text-4xl lg:text-[46px] font-medium gradient-text-red leading-normal tracking-tight" set:html={subtitle} />
    </div>
  )}
</section>

<style>
  @keyframes marquee-banner {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  .animate-marquee-banner {
    animation: marquee-banner 25s linear infinite;
    width: max-content;
  }

  .marquee-clip {
    overflow-x: clip;
    padding-bottom: 0.3em;
  }

  .marquee-text {
    color: #ffffff;
    font-weight: 500;
    letter-spacing: -0.0025em;
  }
</style>
